export const TAU = Math.PI * 2;
export const clamp = (value, min = 0, max = 1) => Math.min(max, Math.max(min, value));
export const lerp = (a, b, t) => a + (b - a) * t;
export const invLerp = (a, b, value) => a === b ? 0 : (value - a) / (b - a);
export const smoothstep = (a, b, value) => { const t = clamp(invLerp(a, b, value)); return t * t * (3 - 2 * t); };
export const smootherstep = (a, b, value) => { const t = clamp(invLerp(a, b, value)); return t * t * t * (t * (t * 6 - 15) + 10); };
export const pulse = (a, b, c, d, value) => smoothstep(a, b, value) * (1 - smoothstep(c, d, value));
export function mulberry32(seed) { let state = seed >>> 0; return () => { state |= 0; state = state + 0x6D2B79F5 | 0; let value = Math.imul(state ^ state >>> 15, 1 | state); value = value + Math.imul(value ^ value >>> 7, 61 | value) ^ value; return ((value ^ value >>> 14) >>> 0) / 4294967296; }; }
export const randRange = (rng, min, max) => lerp(min, max, rng());
export function v3Copy(out, value) { out[0]=value[0]; out[1]=value[1]; out[2]=value[2]; return out; }
export function v3Sub(out, a, b) { out[0]=a[0]-b[0]; out[1]=a[1]-b[1]; out[2]=a[2]-b[2]; return out; }
export function v3Length(value) { return Math.hypot(value[0], value[1], value[2]); }
export function v3Normalize(out, value) { const length=v3Length(value)||1; out[0]=value[0]/length; out[1]=value[1]/length; out[2]=value[2]/length; return out; }
export function v3Lerp(out,a,b,t){out[0]=lerp(a[0],b[0],t);out[1]=lerp(a[1],b[1],t);out[2]=lerp(a[2],b[2],t);return out;}
export function catmullRom(points, t, out=[0,0,0]) { const count=points.length; if(count<2)return v3Copy(out,points[0]||[0,0,0]); const scaled=clamp(t,0,.999999)*(count-1); const index=Math.floor(scaled); const local=scaled-index; const p0=points[Math.max(0,index-1)],p1=points[index],p2=points[Math.min(count-1,index+1)],p3=points[Math.min(count-1,index+2)]; const tt=local*local,ttt=tt*local; for(let axis=0;axis<3;axis++){out[axis]=.5*(2*p1[axis]+(-p0[axis]+p2[axis])*local+(2*p0[axis]-5*p1[axis]+4*p2[axis]-p3[axis])*tt+(-p0[axis]+3*p1[axis]-3*p2[axis]+p3[axis])*ttt);} return out; }
export function sampleCurve(points,samples){const result=new Array(samples);for(let i=0;i<samples;i++)result[i]=catmullRom(points,i/Math.max(1,samples-1),[0,0,0]);return result;}
export function createMat4(){const out=new Float32Array(16);out[0]=out[5]=out[10]=out[15]=1;return out;}
export function mat4Perspective(out,fovy,aspect,near,far){const f=1/Math.tan(fovy/2),nf=1/(near-far);out.fill(0);out[0]=f/aspect;out[5]=f;out[10]=(far+near)*nf;out[11]=-1;out[14]=2*far*near*nf;return out;}
export function mat4LookAt(out,eye,center,up){let x0,x1,x2,y0,y1,y2,z0=eye[0]-center[0],z1=eye[1]-center[1],z2=eye[2]-center[2],length=Math.hypot(z0,z1,z2)||1;z0/=length;z1/=length;z2/=length;x0=up[1]*z2-up[2]*z1;x1=up[2]*z0-up[0]*z2;x2=up[0]*z1-up[1]*z0;length=Math.hypot(x0,x1,x2)||1;x0/=length;x1/=length;x2/=length;y0=z1*x2-z2*x1;y1=z2*x0-z0*x2;y2=z0*x1-z1*x0;out[0]=x0;out[1]=y0;out[2]=z0;out[3]=0;out[4]=x1;out[5]=y1;out[6]=z1;out[7]=0;out[8]=x2;out[9]=y2;out[10]=z2;out[11]=0;out[12]=-(x0*eye[0]+x1*eye[1]+x2*eye[2]);out[13]=-(y0*eye[0]+y1*eye[1]+y2*eye[2]);out[14]=-(z0*eye[0]+z1*eye[1]+z2*eye[2]);out[15]=1;return out;}
export function mat4Multiply(out,a,b){const a00=a[0],a01=a[1],a02=a[2],a03=a[3],a10=a[4],a11=a[5],a12=a[6],a13=a[7],a20=a[8],a21=a[9],a22=a[10],a23=a[11],a30=a[12],a31=a[13],a32=a[14],a33=a[15],b00=b[0],b01=b[1],b02=b[2],b03=b[3],b10=b[4],b11=b[5],b12=b[6],b13=b[7],b20=b[8],b21=b[9],b22=b[10],b23=b[11],b30=b[12],b31=b[13],b32=b[14],b33=b[15];out[0]=b00*a00+b01*a10+b02*a20+b03*a30;out[1]=b00*a01+b01*a11+b02*a21+b03*a31;out[2]=b00*a02+b01*a12+b02*a22+b03*a32;out[3]=b00*a03+b01*a13+b02*a23+b03*a33;out[4]=b10*a00+b11*a10+b12*a20+b13*a30;out[5]=b10*a01+b11*a11+b12*a21+b13*a31;out[6]=b10*a02+b11*a12+b12*a22+b13*a32;out[7]=b10*a03+b11*a13+b12*a23+b13*a33;out[8]=b20*a00+b21*a10+b22*a20+b23*a30;out[9]=b20*a01+b21*a11+b22*a21+b23*a31;out[10]=b20*a02+b21*a12+b22*a22+b23*a32;out[11]=b20*a03+b21*a13+b22*a23+b23*a33;out[12]=b30*a00+b31*a10+b32*a20+b33*a30;out[13]=b30*a01+b31*a11+b32*a21+b33*a31;out[14]=b30*a02+b31*a12+b32*a22+b33*a32;out[15]=b30*a03+b31*a13+b32*a23+b33*a33;return out;}
export function cameraKeyframe(time,keyframes,outEye,outTarget){if(time<=keyframes[0].time){v3Copy(outEye,keyframes[0].eye);v3Copy(outTarget,keyframes[0].target);return;}const last=keyframes[keyframes.length-1];if(time>=last.time){v3Copy(outEye,last.eye);v3Copy(outTarget,last.target);return;}let index=0;while(index<keyframes.length-1&&time>keyframes[index+1].time)index++;const a=keyframes[index],b=keyframes[index+1],tt=smootherstep(a.time,b.time,time);v3Lerp(outEye,a.eye,b.eye,tt);v3Lerp(outTarget,a.target,b.target,tt);}
