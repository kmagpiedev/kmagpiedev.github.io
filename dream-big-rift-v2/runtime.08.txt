  function drawLightThreads(t){const g=fctx,appear=pulse(7.5,10.5,41.5,47.5,t);if(appear<.001)return;const cols=['247,241,223','239,181,67','67,191,213','233,86,66','71,151,102'],flow=smoother(8,20,t),toKnot=smoother(34,46,t);g.save();g.globalCompositeOperation='lighter';g.lineCap='round';for(let i=0;i<5;i++){const seed=i*1.37,p=new Path2D(),sx=lerp(360,220,flow),sy=H*.5+(i-2)*42+Math.sin(t*.32+seed)*18,ex=lerp(1510,W*.5+100,toKnot),ey=lerp(H*.5+(i-2)*35,H*.49+(i-2)*10,toKnot),b1x=630+Math.sin(seed+t*.18)*90,b1y=260+(i%2)*430+Math.cos(t*.23+seed)*50,b2x=1180+Math.cos(seed*2+t*.16)*110,b2y=820-(i%2)*400+Math.sin(t*.2+seed)*48;p.moveTo(sx,sy);p.bezierCurveTo(b1x,b1y,b2x,b2y,ex,ey);const col=cols[i];g.shadowColor=`rgba(${col},.85)`;g.shadowBlur=26;g.strokeStyle=`rgba(${col},${appear*(.18+i*.015)})`;g.lineWidth=10-i*.8;g.stroke(p);g.shadowBlur=8;g.strokeStyle=`rgba(${col},${appear*(.55+i*.025)})`;g.lineWidth=1.5+i*.35;g.stroke(p);for(let j=0;j<24;j++){const q=fract(j/24+t*(.055+i*.008)+seed*.07),x=bezierPoint(sx,b1x,b2x,ex,q),y=bezierPoint(sy,b1y,b2y,ey,q),r=1.2+2.6*Math.pow(Math.sin(q*Math.PI),2);g.fillStyle=`rgba(${col},${appear*(.2+.45*Math.sin(q*Math.PI))})`;g.beginPath();g.arc(x,y,r,0,Math.PI*2);g.fill()}}g.restore()}
  function drawBridgeTiles(t){const a=pulse(36,40,51,56,t);if(a<.001)return;const g=fctx,build=smoother(36,48,t);g.save();g.globalCompositeOperation='lighter';for(let i=0;i<88;i++){const q=i/87,visible=smooth(q-.035,q,build);if(visible<=0)continue;const x=480+q*980,y=H*.59-Math.sin(q*Math.PI)*170+Math.sin(q*18+t*.2)*4,tangent=-Math.cos(q*Math.PI)*170*Math.PI/980,rot=Math.atan(tangent),pal=PALETTE[i%5];g.save();g.translate(x,y);g.rotate(rot);g.globalAlpha=a*visible*(.38+.34*Math.sin(q*Math.PI));g.shadowColor=`rgb(${pal[0]},${pal[1]},${pal[2]})`;g.shadowBlur=18;g.fillStyle=`rgb(${pal[0]},${pal[1]},${pal[2]})`;const w=9+5*Math.sin(q*Math.PI),h=2.2+1.8*Math.sin(q*Math.PI);g.fillRect(-w*.5,-h*.5,w,h);g.restore()}g.restore()}
  function drawHanjiFragments(t){const a=pulse(9,12,31,38,t)+pulse(47,50,57,59,t)*.45;if(a<.001)return;const g=fctx;g.save();g.globalCompositeOperation='lighter';for(let i=0;i<70;i++){const s=hash(i*3.71),q=fract(s+t*(.012+.01*hash(i+4.2))),x=120+q*1680+Math.sin(t*.15+i)*45,y=H*.5+Math.sin(q*Math.PI*3+i*.9)*250+(hash(i+7)-.5)*180,z=.35+.65*hash(i+13),col=PALETTE[i%5];g.save();g.translate(x,y);g.rotate(i*.71+t*(.03+.04*s));g.globalAlpha=a*(.035+.12*z);g.fillStyle=`rgb(${col[0]},${col[1]},${col[2]})`;g.shadowColor=g.fillStyle;g.shadowBlur=12;const w=4+14*z,h=1+4*z;g.fillRect(-w*.5,-h*.5,w,h);g.restore()}g.restore()}
