ctx.createDelay(.8);this.delay.delayTime.value=.31;this.feedback=this.ctx.createGain();this.feedback.gain.value=.23;this.delay.connect(this.feedback).connect(this.delay);this.delay.connect(this.master);this.triggered=new Set()}async start(){if(!this.ctx)return;await this.ctx.resume();this.master.gain.cancelScheduledValues(this.ctx.currentTime);this.master.gain.setTargetAtTime(muted?0:.52,this.ctx.currentTime,.22)}setMuted(v){if(this.ctx)this.master.gain.setTargetAtTime(v?0:.52,this.ctx.currentTime,.18)}reset(){this.triggered.clear()}pluck(freq,gain=.08,duration=2.6){if(!this.ctx)return;const now=this.ctx.currentTime,o=this.ctx.createOscillator(),g=this.ctx.createGain(),f=this.ctx.createBiquadFilter();o.type='triangle';o.frequency.setValueAtTime(freq,now);o.frequency.exponentialRampToValueAtTime(freq*.997,now+duration);g.gain.setValueAtTime(.0001,now);g.gain.exponentialRampToValueAtTime(gain,now+.018);g.gain.exponentialRampToValueAtTime(.0001,now+duration);f.type='lowpass';f.frequency.value=1400;o.connect(f).connect(g);g.connect(this.master);g.connect(this.delay);o.start(now);o.stop(now+duration+.1)}bell(freq,gain=.12,duration=4){if(!this.ctx)return;const now=this.ctx.currentTime;[1,2.01,3.98].forEach((m,i)=>{const o=this.ctx.createOscillator(),g=this.ctx.createGain();o.type='sine';o.frequency.value=freq*m;g.gain.setValueAtTime(gain/(1+i*1.5),now);g.gain.exponentialRampToValueAtTime(.0001,now+duration/(1+i*.35));o.connect(g).connect(this.master);g.connect(this.delay);o.start(now);o.stop(now+duration+.1)})}wingbeat(){if(!this.ctx)return;const now=this.ctx.currentTime,b=this.ctx.createBuffer(1,this.ctx.sampleRate*.8,this.ctx.sampleRate),d=b.getChannelData(0);for(let i=0;i<d.length;i++){const env=Math.sin(Math.PI*i/d.length);d[i]=(Math.random()*2-1)*env*env}const s=this.ctx.createBufferSource(),f=this.ctx.createBiquadFilter(),g=this.ctx.createGain();s.buffer=b;f.type='lowpass';f.frequency.setValueAtTime(180,now);f.frequency.exponentialRampToValueAtTime(70,now+.8);g.gain.value=.16;s.connect(f).connect(g).connect(this.master);s.start(now)}update(t,open,storm,seal){if(!this.ctx)return;const now=this.ctx.currentTime;this.lowpass.frequency.setTargetAtTime(540+open*700+seal*380,now,.16);this.noiseGain.gain.setTargetAtTime(.008+storm*.035+seal*.015,now,.18);this.oscs[0].g.gain.setTargetAtTime(.08+seal*.035,now,.2);for(const[at,type,f]of[[1,'bell',146.83],[8,'pluck',293.66],[12,'pluck',367],[18,'bell',98],[25,'pluck',440],[29,'wing',0],[34,'pluck',587.33],[39,'pluck',733.9],[44,'bell',146.83],[49,'pluck',587.33],[52,'pluck',733.9],[55,'bell',220]]){const key=String(at);if(t>=at&&t<at+.28&&!this.triggered.has(key)){this.triggered.add(key);if(type==='bell')this.bell(f,.09,4.8);else if(type==='wing')this.wingbeat();else this.pluck(f,.07,2.5)}}}}
  function ensureAudio(){if(!audio)audio=new AudioEngine();return audio}
