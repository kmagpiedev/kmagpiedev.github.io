a=.075;g.fillStyle=noisePattern;g.fillRect(0,0,W,H);g.restore();g.save();g.globalAlpha=.055;g.strokeStyle='rgba(247,241,223,.42)';g.lineWidth=.7;for(let i=0;i<38;i++){const y=(i*97+Math.sin(i*4.3)*57+t*2.2)%H;g.beginPath();g.moveTo(-80,y);g.bezierCurveTo(450,y+Math.sin(i)*18,1320,y-23,2020,y+Math.cos(i)*17);g.stroke()}g.restore();const moonA=(1-smooth(7,14,t))+smooth(49,57,t)*(1-smooth(58.3,60,t));if(moonA>.001){const mx=W*.5,my=H*.47;g.save();g.globalCompositeOperation='screen';let gr=g.createRadialGradient(mx,my,0,mx,my,420);gr.addColorStop(0,`rgba(255,244,211,${.26*moonA})`);gr.addColorStop(.28,`rgba(91,209,221,${.08*moonA})`);gr.addColorStop(1,'rgba(0,0,0,0)');g.fillStyle=gr;g.fillRect(mx-430,my-430,860,860);gr=g.createRadialGradient(mx,my,0,mx,my,230);gr.addColorStop(0,`rgba(255,250,229,${.36*moonA})`);gr.addColorStop(1,'rgba(255,246,218,0)');g.fillStyle=gr;g.beginPath();g.arc(mx,my,245,0,Math.PI*2);g.fill();g.restore()}const riftOpen=pulse(14,19,44,51,t);g.save();g.globalCompositeOperation='screen';for(let i=0;i<stars.length;i++){const s=stars[i],tw=.48+.52*Math.sin(t*(.25+s.z*.8)+s.phase),dx=Math.abs(s.x-W*.5),absence=1-riftOpen*smooth(180,40,dx),a=(.12+s.z*.48)*tw*absence*(.78+day*.4),col=s.hue<.12?'239,181,67':s.hue<.34?'91,211,222':'238,244,255';g.fillStyle=`rgba(${col},${a})`;g.beginPath();g.arc(s.x+Math.sin(t*.025+s.phase)*s.z*8,s.y,s.r*(.8+tw*.4),0,Math.PI*2);g.fill()}g.restore()}
  function riftPath(t,offset=0){const p=new Path2D(),open=pulse(14,19,43,51,t),drift=Math.sin(t*.13)*15;for(let y=-80;y<=H+80;y+=18){const n=fbm1(y*.007+t*.11)*34+Math.sin(y*.021+t*.33)*16,x=W*.5+drift+n+offset;if(y===-80)p.moveTo(x,y);else p.lineTo(x,y)}return{p,open}}
  function drawRift(t){const g=fctx;g.clearRect(0,0,W,H);const{p,open}=riftPath(t,0);if(open<.001)return;const seal=pulse(35,40,47.5,52,t),width=(54+open*130)*(1-seal*.72);g.save();g.globalCompositeOperation='source-over';g.strokeStyle=`rgba(1,4,12,${.88*open})`;g.lineWidth=width;g.lineCap='round';g.shadowColor='rgba(0,0,0,.92)';g.shadowBlur=48;g.stroke(p);g.shadowBlur=0;g.strokeStyle=`rgba(8,10,18,${.96*open})`;g.lineWidth=width*.62;g.stroke(p);g.globalCompositeOperation='lighter';const left=riftPath(t,-width*.48).p,right=riftPath(t,width*.48).p;g.lineWidth=2.2+open*3.2;g.shadowBlur=24;g.strokeStyle=`rgba(104,225,235,${.55*open*(1-seal)})`;g.shadowColor='rgba(67,191,213,.85)';g.stroke(left);g.stroke(right);g.strokeStyle=`rgba(248,241,221,${.36*open*(1-seal)})`;g.lineWidth=1.1;g.stroke(left);g.stroke(right);if(seal>.001){g.strokeStyle=`rgba(245,193,74,${.68*seal})`;g.shadowColor='rgba(239,181,67,.82)';g.shadowBlur=22;g.lineWidth=2.4+seal*2.8;g.stroke(p);g.strokeStyle=`rgba(255,247,213,${.72*seal})`;g.shadowBlur=12;g.lineWidth=1.4;g.stroke(p)}g.restore()}
