  function currentTime(now){if(frameMode)return clamp(forcedFrame,0,DURATION-.001);if(debugMode&&paused)return pauseAt;if(!running||paused)return pauseAt;return((now-startStamp)/1000)%DURATION}
  function begin(restart=true){const now=performance.now();if(restart||!running)pauseAt=0;startStamp=now-pauseAt*1000;running=true;paused=false;intro.classList.add('hidden');document.body.classList.add('playing');ensureAudio()?.start();audio?.reset();lastInteraction=now}
  function replay(){pauseAt=0;startStamp=performance.now();running=true;paused=false;audio?.reset();intro.classList.add('hidden');document.body.classList.add('playing')}
  function updateMuteLabel(){muteButton.classList.toggle('muted',muted);const span=muteButton.querySelector('span');if(span)span.textContent=muted?COPY[lang].unmute:COPY[lang].mute;audio?.setMuted(muted)}
  startButton.addEventListener('click',()=>begin(true));replayButton.addEventListener('click',replay);fullscreenButton.addEventListener('click',async()=>{try{if(!document.fullscreenElement)await document.documentElement.requestFullscreen();else await document.exitFullscreen()}catch{}});muteButton.addEventListener('click',()=>{muted=!muted;updateMuteLabel();lastInteraction=performance.now()});['pointermove','pointerdown','keydown','touchstart'].forEach(type=>addEventListener(type,()=>{lastInteraction=performance.now();document.body.classList.remove('ui-idle')},{passive:true}));
  if(debugMode){debugPanel.hidden=false;intro.classList.add('hidden');document.body.classList.add('playing');timelineInput.value=String(pauseAt);debugTime.value=pauseAt.toFixed(2);timelineInput.addEventListener('input',()=>{pauseAt=Number(timelineInput.value);paused=true;running=true;debugTime.value=pauseAt.toFixed(2)});debugPlay.addEventListener('click',()=>{if(paused){startStamp=performance.now()-pauseAt*1000;paused=false}else{pauseAt=currentTime(performance.now());paused=true}})}
  if(captureMode){document.body.classList.add('capture');intro.classList.add('hidden');running=true;paused=false;startStamp=performance.now()}
  if(frameMode){intro.classList.add('hidden');document.body.classList.add('capture');running=true;paused=true;pauseAt=clamp(forcedFrame,0,DURATION-.001)}
  updateMuteLabel();
  function present(){const w=canvas.width/dpr,h=canvas.height/dpr;ctx.setTransform(dpr,0,0,dpr,0,0);ctx.fillStyle='#06142b';ctx.fillRect(0,0,w,h);ctx.save();ctx.translate(viewX,viewY);ctx.scale(viewScale,viewScale);ctx.drawImage(sceneLayer,0,0);ctx.restore()}
